name: "react-sample-ci-cd"

on: 
    push:
        branches:
            - master
            - test

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
          - name: "setup node js"
            uses: actions/setup-node@v4
            with: 
              node-version: "22"